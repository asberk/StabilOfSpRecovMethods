%% Compare methods, stratified by noise. 
% This should only be run *after* RelationshipBetweenConstants has been
% run!

num_b = size(b,2);
n_cols = round(sqrt(num_b));
n_rows = ceil(num_b/n_cols);


